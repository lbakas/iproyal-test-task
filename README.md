After cloning repository run database migration:
```
php artisan migrate
```
After migration populate database with fake data:
```
php artisan db:seed
```
After populating database launch the app:
```
php artisan serve
```